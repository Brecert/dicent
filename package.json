{
  "name": "dicent",
  "version": "0.0.1",
  "description": "Decent, a decentrialized, federated chat platform",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "pretty": "prettier typescript --write 'src/**/*.ts'",
    "pretty:watch": "onchange '**/*.ts' -- prettier typescript --write {{changed}}"
  },
  "devDependencies": {
    "onchange": "^5.1.3",
    "prettier": "^1.15.3",
    "typescript": "^3.2.1"
  }
}
